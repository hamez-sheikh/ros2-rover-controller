{
  "name": "ROS 2 Jazzy",
  "image": "ros:jazzy-ros-base",
  "remoteUser": "root",
  "postCreateCommand": "apt-get update && apt-get install -y python3-colcon-common-extensions ros-jazzy-demo-nodes-py && rm -rf /var/lib/apt/lists/* && echo 'source /opt/ros/jazzy/setup.bash' >> /root/.bashrc",
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ]
    }
  }
}