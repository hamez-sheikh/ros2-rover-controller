"""PinProof — Week 1 evidence prototype.

A deterministic, local-only checker that cross-checks firmware pin
configuration (PlatformIO / Arduino-ESP32) against a KiCad XML netlist,
via a role contract (pinproof.yaml) and a module rulepack.
"""

__version__ = "0.1.0"
